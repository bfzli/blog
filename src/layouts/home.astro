---
import type { Props } from '@/types'
import { Page } from '@/layouts'
import { HomeHero, ThemeToggle } from '@/components'

const props = Astro.props as Props
---

<Page {...props}>
    <HomeHero>
        <ThemeToggle />
    </HomeHero>

    <slot />
</Page>