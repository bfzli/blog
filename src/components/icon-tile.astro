---
import { Image } from 'astro:assets'

interface Props {
    src: ImageMetadata
    alt: string
    size?: number
}

const { src, alt, size = 48 } = Astro.props
---

<div
    class='rounded-2xl bg-elevate flex items-center justify-center'
    style={`width:${size}px;height:${size}px`}
>
    <Image
        src={src}
        alt={alt}
        width={size}
        height={size}
        class='w-full h-full rounded-2xl object-contain'
    />
</div>
